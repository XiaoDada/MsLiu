<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dafeng.msliu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".touchtest.FirstActivity" />
        <activity android:name=".touchtest.SecondActivity" />
        <activity android:name=".touchtest.ThreeActivity" />
        <activity android:name=".touchtest.FourActivity" />
        <activity android:name=".touchtest.FiveActivity" />
        <activity android:name=".touchtest.SixActivity" />
        <activity android:name=".touchtest.MorePointViewActivity" />
        <activity android:name=".function.activity.ViewpagePagingActivity" />
    </application>

</manifest>